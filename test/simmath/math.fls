
pub class Math {
    fn math() -> self {
        return self;
    }

    pub fn add(a: i64, b: i64) -> i64 {
        return a + b;
    }

    pub fn sub(a: i64, b: i64) -> i64 {
        return a - b;
    }
}